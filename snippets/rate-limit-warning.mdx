<Warning>
Usage is governed by your plan's monthly credits — exhaustion surfaces as `429` with `code: "quota_exceeded"` on image/GIF/template/batch endpoints and `402` on video/workflow endpoints; neither is retryable. Fixed per-minute limits on a few public endpoints send standard `x-ratelimit-*` headers and are retryable after backing off.
</Warning>
