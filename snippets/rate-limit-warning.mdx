<Warning>
Usage is governed by your plan's monthly credits (`402` when exhausted) and, on a few public endpoints, fixed per-minute rate limits (`429`). No rate-limit headers are sent — on `429`, back off exponentially starting at a few seconds.
</Warning>
